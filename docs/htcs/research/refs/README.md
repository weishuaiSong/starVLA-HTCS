# Reference papers

PDFs themselves are not tracked in this repo (kept out of git to avoid
bloat). Links below; download to a local cache if you need offline copies.

| Short name | Paper | Where it informs HTCS |
|---|---|---|
| `hif-vla` | _TODO: paste arXiv / OpenReview link_ | Motivation for hierarchical history compression in VLA |
| `onevision-encoder` | _TODO: paste arXiv / HuggingFace link_ | Signal-level pattern we borrow (saliency-based patch selection); architectural distinction logged in `docs/htcs/新工作-语言条件化历史压缩-工作文档.md` §7.2.1 |

To add a new reference: append a row and (optionally) drop the pdf into
this folder locally — `.gitignore` keeps `*.pdf` out of commits.
